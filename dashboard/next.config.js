/** @type {import('next').NextConfig} */
const nextConfig = {
  // Copy pipeline outputs into public directory at build time
  // Run: cp -r ../output ./public/data before build
}

module.exports = nextConfig
